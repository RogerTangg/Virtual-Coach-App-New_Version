# 🚀 快速修復資料庫問題

## 問題診斷
錯誤訊息：**"Could not find the table 'public.exercises' in the schema cache"**

這表示 Supabase 資料表尚未建立。

## 🔧 解決方案（3 分鐘完成）

### 方法 1: 使用 Supabase Dashboard（推薦）

1. **開啟 Supabase Dashboard**
   - 網址：https://supabase.com/dashboard
   - 登入你的帳號
   - 選擇專案：`virtual-coach-app` 或 ID 為 `qdpurcqksmmymuvbjnvo` 的專案

2. **執行 SQL 腳本**
   - 點擊左側選單的 **「SQL Editor」**
   - 點擊 **「New query」**
   - 開啟檔案：`database/setup.sql`
   - 複製全部內容（Ctrl+A, Ctrl+C）
   - 貼上到 SQL Editor 中（Ctrl+V）
   - 點擊右上角 **「Run」** 按鈕（或按 Ctrl+Enter）

3. **確認執行成功**
   - 應該看到綠色訊息：✅ Success
   - 最後會顯示統計表格，顯示各肌群的運動數量

4. **驗證資料表**
   - 點擊左側選單的 **「Table Editor」**
   - 你應該看到 `exercises` 資料表
   - 點擊進入，應該有 25 筆運動資料

5. **重新載入應用程式**
   ```bash
   # 在終端機中按 Ctrl+C 停止開發伺服器
   # 然後重新啟動
   npm run dev
   ```
   - 重新整理瀏覽器（F5）

### 方法 2: 使用 Supabase CLI（進階）

如果你有安裝 Supabase CLI：

```bash
# 1. 登入
supabase login

# 2. 連接到專案
supabase link --project-ref qdpurcqksmmymuvbjnvo

# 3. 執行腳本
supabase db push

# 4. 或直接執行 SQL 檔案
psql -h db.qdpurcqksmmymuvbjnvo.supabase.co -U postgres -d postgres -f database/setup.sql
```

## ✅ 完成後的檢查清單

- [ ] Supabase Dashboard 可以看到 `exercises` 資料表
- [ ] 資料表中有 25 筆運動資料
- [ ] 應用程式可以正常載入，沒有錯誤訊息
- [ ] 可以選擇偏好並生成訓練計畫

## 📊 預期結果

完成後，你的資料庫將包含：

```
目標肌群分布：
├─ 腿部 (legs):      4 個運動
├─ 胸部 (chest):     4 個運動
├─ 核心 (core):      4 個運動
├─ 背部 (back):      3 個運動
├─ 肩膀 (shoulders): 4 個運動
├─ 手臂 (arms):      3 個運動
└─ 全身:             3 個運動
────────────────────────────
總計:                25 個運動

難度分布：
├─ 初學者 (beginner):     16 個
├─ 中階 (intermediate):   8 個
└─ 進階 (advanced):       1 個
```

## 🐛 還是有問題？

### 問題 1: SQL 執行失敗
- **原因**: 權限不足或語法錯誤
- **解決**: 確認你是專案擁有者，或聯繫專案管理員

### 問題 2: 資料表建立了但應用程式還是看不到
- **解決**: 
  1. 清除瀏覽器快取（Ctrl+Shift+Delete）
  2. 重新啟動開發伺服器
  3. 檢查 `.env` 檔案中的 URL 和金鑰是否正確

### 問題 3: 連接超時
- **原因**: 網路問題或 Supabase 服務暫時不可用
- **解決**: 
  1. 檢查網路連接
  2. 前往 https://status.supabase.com/ 查看服務狀態
  3. 稍後再試

## 📞 需要協助？

參考完整指南：`database/README.md`
